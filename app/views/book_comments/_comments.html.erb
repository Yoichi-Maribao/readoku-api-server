<p>コメント件数：<%= book.book_comments.count %></p>
<% book.book_comments.each do |book_comment| %>
<div class="mb-3">
  <%= image_tag book_comment.user.get_profile_image(40, 40), class: "attachment user profile_image fallback" %>
  <%= book_comment.user.name %>
  <%= book_comment.created_at.strftime('%Y/%m/%d') %><%= book_comment.comment %>
  <% if book_comment.user == current_user %>
    <%= link_to "削除", book_book_comment_path(book_comment.book, book_comment), method: :delete, remote: true, class: "btn btn-sm btn-danger" %>
  <% end %>
</div>
<% end %>