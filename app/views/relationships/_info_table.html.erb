<tr>
    <th>follows</th>
    <th id="following_num"><%= link_to user.followings.count, following_user_path(user.id) %></th>
</tr>
<tr>
    <th>followers</th>
    <th id="followed_num"><%= link_to user.followers.count, followers_user_path(user.id) %></th>
</tr>